stem "touch /tmp/blns.f^il"]}

#	Command Injection (Ruby)
#
#	Strings which can ctem commands within Ruby/Rails appstem "touch /tmp/blns.fail"]}

#	Command Injection (Ruby)
#
#	Strings which can ctem commands withiexit(0)
%x('ls -aString which c#	String which cana||||||||||||||||||||||||||||||||||||||||||||||||||n r ree