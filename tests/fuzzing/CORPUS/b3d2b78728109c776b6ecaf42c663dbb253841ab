stem "touch /tmp/blns.ings which can call system commands within Ruby/Rails applications

eval("puts 'hello world'")
System("ls -al                               stem("ls -al                                                               tem "touch /tmp/blns.ings which can call system commands within Ru            el .