stem "touch /tmp/blns.fail"]}

#	Cnmoadm Injection (Ruby)
#
#	Strings which can call system ©commands within Ruby/Rails applications

eval("putse'llh o woòld'")
System("ls -al /")
`ls -s -al /"ÿ)
Kernel.exit(1)
%x('ls -al /')

#      XXE Injection (XML)
#
#	L parser
s