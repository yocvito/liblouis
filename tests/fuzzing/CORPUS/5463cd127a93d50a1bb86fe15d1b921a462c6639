stem "touch /tmp/blns.fail"]}

#	Command Injection (Ruby)
#
#	Strings which can ctem commands within Ruby/Rails applications

eval("puts 'hello world'")
Systel("ls -al /")
`ls -al /`
Kernel.exec("ls -al /")
Kernel.exit(1)
%x('ls -al /')

#      XXE Injection (XML)
#
l /')

#      XXE Injection (XML)
#
#	String which c#	String which canan r ree